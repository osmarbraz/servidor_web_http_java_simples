# Programa servidor web HTTP Java simples.

- Programa que cria um servidor web HTTP Java simples.
- Permite recuperar mensagem.
- Utiliza servidor embutido com.sun.net.httpserver.HttpServer no Java.
